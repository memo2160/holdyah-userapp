<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Track Your Hold</title>
    <!-- Bootstrap CSS CDN -->
    <link
        href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/css/bootstrap.min.css"
        rel="stylesheet"
    >
    <link rel="stylesheet" href="css/style.css">

</head>
<body>

    <div class="d-flex vh-100 justify-content-center align-items-center">
      
        <div class="w-100" style="max-width: 500px;">
          <% if ((error_message && error_message.length > 0) || (message && message.length > 0)) { %>
            <div class="alert text-center <%= error_message ? 'alert-danger' : 'alert-success' %>" id="error_alert" role="alert">
              <p><%= error_message ? error_message[0] : message[0] %></p> <!-- Display the first error or success message -->
            </div>
          <% } %>
          <img src="images/holdyah_banner.png" class="img-fluid" style="max-width: 350px;" id="banner">
          <h1 class="mb-4 text-center">Search For Your Hold Order</h1>
          <form action="/submit" method="POST" class="card p-4 shadow-sm w-100" id="pin-form">
            <!-- Surname Field -->
            <div class="mb-3">
              <label for="surname" class="form-label">Last Name</label>
              <input type="text" class="form-control" id="surname" name="surname" placeholder="Enter your lastname" required>
            </div>
      
            <!-- PIN Number Field -->
            <div class="mb-3">
              <label for="pin" class="form-label">Hold Pin</label>
              <input type="password" class="form-control" id="pin" name="pin" placeholder="Enter your PIN number" required>
            </div>
      
            <!-- Submit Button -->
            <button type="submit" class="btn btn-primary w-100">Submit</button>
          </form>
        </div>
      </div>
      
      
    <!-- Bootstrap JS CDN (Optional for some components) -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
